1. list start with 0 and it also start with negative so the list reverses on -1.
2. append() = to add element in last or -1.
3. pop() = removes last element i.e -1 index.
4. index() = returns the index element.
5. insert() = inserts element to designated destination.
6. remove() = removes element 
7. reverse() = reverses the list
8. a[1:7] = returns another list from index to 6.
9. sort() = returns element by sorting it.